# app/schemas — edge contracts

Pydantic request/response models only. Map to/from ORM rows in services with `model_validate`/`model_dump`; never use these as tables and never use tables as response models.
