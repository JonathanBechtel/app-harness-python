"""Optional server-rendered web layer (Jinja + static assets, no build step).

An API-only project leaves this package as-is; it registers one placeholder
route and costs nothing. A web-facing project adds page routes here following
app/web/CLAUDE.md. Either way it is the same repo and the same stack.
"""
